using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Suit : MonoBehaviour
{
    /*
    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.tag == "Steam")
        {
            Debug.Log("suit entered");
            collision.transform.GetComponent<Steam>().colliders.Add("Suit");
        }
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        if (collision.tag == "Steam")
        {
            collision.transform.GetComponent<Steam>().colliders.Remove("Suit");
            if (collision.transform.GetComponent<Steam>().colliders.Contains("Suit"))
            {
                Debug.Log("Suit exited");
            }
        }
    }
    */
}
